



procedure Hello_World.Setup( Spawn_Tasks : in Boolean );
-- Initialize the Hello World component
-- Spawn_Tasks define if the command is called by a server; and when true it means tasks and
-- other server-only related code should be started up as well
